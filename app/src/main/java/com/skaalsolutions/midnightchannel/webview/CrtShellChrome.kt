package com.skaalsolutions.midnightchannel.webview

/**
 * Shared CRT chrome tokens for WebView surface + blank error documents.
 * Kept in the webview package so clients do not depend on Compose theme types.
 */
internal object CrtShellChrome {
    const val BACKGROUND_HEX: String = "#0A0A0A"
}
